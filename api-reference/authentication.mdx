---
title: Autenticar
description: Gere tokens JWT para autenticação segura na plataforma Super Pagamentos.
---

## Endpoint

**POST** `/auth`  
Autentica sua aplicação na plataforma Super Pagamentos. Ao fornecer sua `publicKey` e `secretKey`, você receberá tokens JWT (`access_token` e `refresh_token`) para uso nas APIs protegidas.

---

## Headers

| Nome           | Tipo   | Obrigatório | Exemplo             |
|----------------|--------|-------------|---------------------|
| Content-Type   | string | Sim         | application/json    |

---

## Body

```json
{
  "publicKey": "suaPublicKeyAqui",
  "secretKey": "suaSecretKeyAqui"
}
```

---

## Exemplos de requisição

### 📦 cURL

```bash
curl -X POST https://api.superpagamentos.com/auth \
  -H "Content-Type: application/json" \
  -d '{
    "publicKey": "suaPublicKeyAqui",
    "secretKey": "suaSecretKeyAqui"
  }'
```

### 🐘 PHP

```php
<?php
$data = [
  "publicKey" => "suaPublicKeyAqui",
  "secretKey" => "suaSecretKeyAqui"
];
$options = [
  'http' => [
    'method'  => 'POST',
    'header'  => "Content-type: application/json",
    'content' => json_encode($data)
  ]
];
$context  = stream_context_create($options);
$result = file_get_contents('https://api.superpagamentos.com/auth', false, $context);
echo $result;
```

### 🟦 Node.js (axios)

```javascript
const axios = require('axios');

axios.post('https://api.superpagamentos.com/auth', {
  publicKey: 'suaPublicKeyAqui',
  secretKey: 'suaSecretKeyAqui'
})
.then(response => console.log(response.data))
.catch(error => console.error(error.response.data));
```

---

## ✅ Resposta 200 - Sucesso

```json
{
  "data": {
    "access_token": "eyJhbGciOiJIUzI1NiIsInR5c...",
    "refresh_token": "eyJhbGciOiJIUzI1NiIsInR5cCI...",
    "expires_in": "3600s"
  },
  "message": "Autenticado com sucesso!"
}
```

---

## ❌ Resposta 400 - Erro

```json
{
  "message": "Credenciais inválidas."
}
```
---

### Observação
Esses tokens devem ser armazenados com segurança e utilizados no header `Authorization` para autenticar chamadas subsequentes.
