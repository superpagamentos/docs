
---
title: Autenticação
description: Gere tokens JWT para autenticação segura na plataforma Super Pagamentos.
---

## 🛡️ Autenticação

**POST** `/auth`

Essa rota é responsável por autenticar sua aplicação junto à plataforma **Super Pagamentos**. Ao fornecer uma `publicKey` e uma `secretKey` válidas, você receberá um par de tokens JWT: um `access_token` para autenticações nas APIs e um `refresh_token` para renovação automática. Esses tokens devem ser utilizados nas requisições subsequentes para garantir acesso seguro e controlado aos recursos da plataforma.

> ⚠️ **Importante:** Guarde suas chaves com segurança. A `secretKey` nunca deve ser exposta publicamente.

---

### 🔐 Requisição

**Headers:**

```http
Content-Type: application/json
```

**Body:**

```json
{
  "publicKey": "suaPublicKeyAqui",
  "secretKey": "suaSecretKeyAqui"
}
```

| Campo       | Tipo    | Obrigatório | Descrição                                |
|-------------|---------|-------------|------------------------------------------|
| `publicKey` | string  | Sim         | Chave pública da sua aplicação           |
| `secretKey` | string  | Sim         | Chave secreta da sua aplicação           |

---

### ✅ Resposta

```json
{
  "data": {
    "access_token": "eyJhbGciOiJIUzI1NiIsInR5c...",
    "refresh_token": "eyJhbGciOiJIUzI1NiIsInR5cCI...",
    "expires_in": "3600s"
  },
  "message": "Autenticado com sucesso!"
}
```

| Campo           | Tipo    | Descrição                                                 |
|------------------|---------|------------------------------------------------------------|
| `access_token`   | string  | Token JWT para autenticação nas rotas protegidas          |
| `refresh_token`  | string  | Token para renovar o `access_token` quando expirar        |
| `expires_in`     | string  | Tempo de validade do token (ex: `"3600s"`)                |
| `message`        | string  | Mensagem de status da autenticação                        |

---

### 💡 Observações

- O `access_token` deve ser utilizado no header `Authorization` das requisições protegidas:

```http
Authorization: Bearer <access_token>
```

- Tokens JWT são assinados com segurança e não devem ser manipulados manualmente.
- A renovação de tokens será feita em uma rota específica de refresh, documentada separadamente.
